name: task testing inside devbox

on:
  workflow_dispatch:

jobs:

  greet:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - uses: jetify-com/devbox-install-action@v0.9.0

      - uses: devbox init

      - run: devbox run -- echo "Hello World!"
